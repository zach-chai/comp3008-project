<%= form_for(@registered_course) do |f| %>
  <% if @registered_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registered_course.errors.count, "error") %> prohibited this registered_course from being saved:</h2>

      <ul>
      <% @registered_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
